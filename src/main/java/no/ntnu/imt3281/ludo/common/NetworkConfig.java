package no.ntnu.imt3281.ludo.common;

/**
 * Configuration class to ensure that we always specify the correct networking
 * configuration when we create network related objects.
 */
public class NetworkConfig {
    public static final int LISTENING_PORT = 9010;
}
